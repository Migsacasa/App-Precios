/**
 * Canonical re-export of the store-evaluation prompt builder.
 *
 * New code should import from here:
 *   import { buildStoreEvaluationPrompts } from "@/lib/ai/prompts/storeEvaluation.prompt";
 */
export { buildStoreEvaluationPrompts } from "@/lib/schemas/prompts";
